version: '3.8'

services:
  yoda-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - AI_GATEWAY_KEY=${AI_GATEWAY_KEY:-default_key}
