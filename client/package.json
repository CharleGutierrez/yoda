{
  "name": "yoda-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:gleam": "gleam build",
    "dev": "npm run build:gleam && vite",
    "build": "npm run build:gleam && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
